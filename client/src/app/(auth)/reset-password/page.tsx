'use client';

const ResetPasswordPage = () => {
  return (
    <div className='flex h-screen w-full items-center justify-center'>
      {/* The Authenticator component handles the reset password flow */}
      {/* Users are redirected here after clicking the reset link or entering code */}
    </div>
  );
};

export default ResetPasswordPage;
